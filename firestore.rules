rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /_health/{doc} {
      allow write: if true;
      allow read: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
